spring:
  application:
    name: user
  profiles:
    active: dev

---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8010/
      failFast: true
      enabled: true
      label: master
      profile: dev


---
spring:
  profiles: prod
  cloud:
    config:
      uri: http://localhost:8010/
      profile: prod
      label: master
      failFast: true
      enabled: true